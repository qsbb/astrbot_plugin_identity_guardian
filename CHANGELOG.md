# Changelog

遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 当前系列归属：知、言、序、情、境、声、核、临；下方版本号与日期均为真实历史记录，不因当前文档整改而改写。

## [Unreleased]

### 修复

- 申请推送与旧版待审通知按群去重：入群申请进入人工待审时，事件驱动推送覆盖的群不再重复收到旧模板通知（`JoinNotificationService.notify` 新增 `exclude_group_ids`，目标解析抽为共享的 `resolve_push_targets`），同一群只收一条消息。
- 群内引用推送消息的审批回复消费后即调 `stop_event`，不再继续进入主对话 LLM 造成同一条消息被回复两次；bot 自己引用推送消息的回显在审批链路入口直接忽略。
- 「模拟申请」结果区不显示推送文案/结果回复预览：`app.js` 中 `renderSimulateResult` 被重复定义，后定义的旧版（无预览渲染）因函数声明提升覆盖了带预览的新版；删除旧版重复定义。

## [0.4.2] - 2026-08-18

### 新增

- 「模拟申请」诊断新增推送文案预览：模拟结果为转人工待审（会触发推送）时，结果区展示该申请实际会推送的文案——与生产推送共用同一渲染路径（`render_push_preview`），formatted 展示模板全文（含看法行与引用回复审批引导），natural 真实调用 `push_llm_provider` 生成并带入目标群会话人格与最近 ≤10 条群消息上下文（取不到静默跳过），natural 失败回退模板时标注 `natural_fallback_formatted`；预览附样式徽章与人格/LLM/群消息条数元信息，只生成不发送。
- 「模拟申请」诊断新增审批结果回复预览：转人工待审时结果区展示「若同意/若拒绝」两种结局下 bot 的回应——生产结果回复的文案生成抽为 `_render_result_reply`（`_send_result_reply` 改为调它再发送，模拟预览调它只取文案，同一实现不漂移），LLM 空/失败时展示固定回退文案并标注 `fallback`/`模板`；预览经新 `result_reply_preview` 钩子注入，钩子异常不影响主诊断结果。
- 入群审核 Page 新增「全局设置」卡片与新路由 GET `settings` / POST `settings/update`：可在 Page 查看并修改审核模型（`audit_llm_provider`，下拉列举当前实例可用 LLM provider，含「默认（主对话 LLM）」项，列表获取失败时降级显示当前生效值）与知联动开关（`enable_active_learner_recall`）；写回沿用 `save_config_async` 原子提交 + 提交后重建 Config 的模式，运行态立即生效，失败不落盘并返回可读错误。

### 变更

- 申请推送两种样式现在都带 bot 自己的看法：formatted 样式不再只附自动审核结论行——LLM caller 可用时先调 `push_llm_provider` 生成一句话看法（新增 `build_push_opinion_prompt`，纯文本、不超过 80 字、折叠为单行并截断 120 字），LLM 不可用/失败/空结果时回退原有自动审核结论行（未经过自动审核时如实标注），行为不变；natural 整段生成已含看法，不重复调用，两种样式合计最多一次额外 LLM 调用。渲染结果新增 `opinion_source`（llm/decision/none）标注，模拟预览小字同步展示看法来源；生产推送与模拟预览共用 `render_push_preview` 自动一致。
- 推送与审批结果回复改为全链路人设生成：natural 推送提示词重写为「只给事实」（昵称/QQ/等级/问答/来源群 + 场景说明），措辞、结构、语气由 LLM 按人设自由发挥，唯一硬性要求是引导管理员「引用本条消息回复同意或拒绝」（引用审批机制依赖它），字数上限放宽到 300；审批结果回复（已同意/已拒绝/已被处理/失败）新增 `build_result_reply_prompt`，由 push LLM 按目标群人格措辞，LLM 不可用/失败/空结果时回退原有固定文案；`push_style` 默认值由 formatted 改为 natural（新建/未显式配置的群生效，已保存配置不受影响，LLM 不可用时渲染层本就有模板回退）。

## [0.4.1] - 2026-08-18

### 新增

- 入群审核 Page 新增「模拟申请」诊断卡片与新路由 POST `simulate`：选群、输入问题（可留空）与答案后走真实三段自动审核链路（真实调用审核 LLM 与知识检索），逐步返回每段判定细节与最终结论，并按该群当前开关说明实际事件会发生什么（批准/保持待审/转人工并推送）；全程零副作用——不触平台批准 API、不写待审记录、不发通知/推送、不写审计日志；群校验复用现有已加入群与审核权限检查。

## [0.4.0] - 2026-08-18

### 新增

- 入群问答预设移到 Page 按群配置：`GroupReviewConfig` 新增 `join_questions`（每项 `question` 可留空表示匹配任意入群问题，`answers` 去空去重、上限 50 条预设）；悬浮窗新增「入群问答预设」编辑区（可增删多条，答案每行一个），随 `groups/update` 保存，批量操作保留该字段；旧全局 `join_questions` 保留，作为按群未配置时的回退。
- 自动审核链路重排为严格三段顺序：预设判定（按群预设优先、全局回退；先精确/模糊匹配，不中再 LLM 对预设答案语义比对）→ 知联动判定（有知识证据才调 LLM）→ 兜底 UNCERTAIN 转忽略/人工；入群问题直接取自申请事件解析结果，不再要求预设问题精确命中；无参考答案且无知识证据时不再让 LLM 自由判断，知识查询失败改为进入兜底而非返回 UNAVAILABLE。
- 新增事件驱动的入群申请推送：申请进入人工待审（未被自动批准）时，自动把申请推送到申请所属群在入群审核 Page 配置的「推送群」（按群 `push_group_ids`；留空时回退推送到申请所属群本身）；样式由按群 `push_style` 决定（formatted 模板 / natural 自然语言，LLM 生成带人设风格、可用新配置 `push_llm_provider` 指定模型、失败自动回退模板）；同一申请对同一推送群幂等只推一次，紧急停止/熔断期间不推送。
- 推送内容带「bot 的看法」并支持群内引用回复审批：formatted 模板追加自动审核结论行（未经过自动审核时如实标注）与审批引导，natural 提示词要求 LLM 根据问答质量给出判断并询问管理员意见；推送改走 OneBot `send_group_msg` 以拿到 message_id 并按申请记录 `push_refs` 映射（随待审 TTL 一起过期）；管理员/群主或 bot 主人在群内引用推送消息回复「同意/不同意」即可审批——语义由审核 LLM 判断（`audit_llm_provider`），含糊不处理；审批复用 `process_request` 的护栏、bot 角色复查与一次性语义，结果回复到群里（已同意/已拒绝/已被处理/失败原因）。
- 入群审核按群配置新增 `pinned`（仅 Page 群配置表置顶展示）、`push_group_ids`（申请推送目标群列表，须为 Bot 已加入的群）与 `push_style`（formatted/natural）；单个与批量更新接口及 Page UI 同步支持。

### 变更

- 入群审核 Page 群配置表瘦身：表格只保留选择、群（可点击）、Bot/权限、状态摘要与操作列；自动审核、发送审核、通知位置、指定审核群白名单、显示答案、置顶、推送群、推送样式等细分设置全部移入点击群名打开的悬浮窗（ESC/点击外部关闭，一次只开一个，保存仍走单群更新接口），窄屏下悬浮窗近全宽。
- 入群审核 Page 页面美化：调整样式与布局，无行为变更。
- `set_member_title` 设置头衔前做友好校验，目标或参数不合法时返回可读错误；该高风险操作接入人工二次确认。
- `confirm_mute_threshold` 默认值由 3600 下调为 600：原默认值大于 `max_mute_seconds`（1800），禁言时长先被钳制到硬上限，人工确认阈值永不可达。
- README 与实现对齐：工具数更正为 14 个；如实标注规则预筛、独立 LLM 审核、动作冷却、分级限频与批量操作检测为预留或未实现；API 层硬拦截改为只声明校验紧急停止与全局熔断；黑名单描述改为在工具调用授权路径生效、与保护名单冲突时保护优先。

### 修复

- `delete_message` 增加请求者校验，防止借 LLM 撤回无权撤回的消息。
- 管理员变更事件监听修正，bot 或群成员的管理员身份变化能正确刷新身份缓存。
- 二次确认流程引入 claim/finish 事务语义：先原子领取待确认条目再执行，完成后落终态，避免并发审批重复执行或异常后条目悬挂。
- `mute_member` 增加禁言时长下限校验，非法或过小时长不再直接透传平台。

### 安全

- 黑名单与保护名单冲突时保护优先：同时在两份名单中的用户不会被黑名单规则踢出。
- Page 入群人工批准/驳回接入紧急停止与全局熔断：紧急停止或熔断期间 Page 动作被拒绝。

## 0.3.5 - 2026-08-17

### 新增

- 新增独立 `pages/join_review` 入群审核页，可只读刷新当前 aiocqhttp Bot 已加入的群，并按 `platform_id + group_id` 配置自动审核、人工通知、通知位置、指定审核群白名单和答案显示。
- 新增持久化人工待审队列与 Page 批准/驳回动作；申请使用 opaque ID，OneBot `flag` 只在服务端内部保存，平台成功后才更新最终状态。

### 变更

- 自动审核与发送审核改为两个独立的按群开关。两者同时开启时先自动审核，任何未实际批准结果均转人工；两者默认关闭且旧全局配置不会自动扩散。
- 旧 `join_audit_mode` 只作为 Page 显式迁移来源，`join_questions`、阈值和审核 LLM Provider 继续作为自动审核模板。

### 安全

- Page API 使用 AstrBot Dashboard 认证扩展路由，保存配置及人工动作均重新校验 Bot 当前群权限；前端不能提交 OneBot `flag` 或临时指定配置外通知目标。
- 通知按申请和目标幂等，失败不删除待审记录；Page、群消息和 OneBot 操作日志均不暴露内部敏感字段。
- 移除旧版接受任意 OneBot `flag` 的 LLM 审核工具；热重载残留工具也会被清理。

### 测试

- 增加四种开关组合、平台失败、并发动作、TTL、通知幂等、按 Bot/群隔离、群发现、批量配置、Page busy 状态与移动布局覆盖。

## 0.3.4 - 2026-08-09

### 新增

- 新增 `identity.quest_binding_control@1.0`，供“临”等受信任后端为已认证 Quest principal 保存精确的私聊只读身份绑定；请求只接受 principal 摘要、client、platform、bot、user 五项，响应只返回状态与计数。
- 新增独立配置 `quest_session_read_only_bindings`，只保存 `qrb1` 不可逆整组摘要，与主人绑定完全分离；同客户端旧 Quest owner 绑定会迁移移除，但不会删除或新增 `owner_users`。

### 安全

- Quest 只读绑定不会写入或修改 `owner_users`，不会授予主人、管理、群操作或其他平台权限。会话授权命中该绑定时明确返回 `owner_confirmed=false`、`grants_platform_action=false`；新列表拒绝旧明文 principal 格式，并支持按 principal/client 正式撤销。原有 `upsert_quest_owner_binding()` 行为保持不变，只用于管理员明确配置主人身份。
- 新接口存在但不兼容、关闭、保存失败或并发修订被覆盖时失败关闭，消费者不得与本地配置合并放行。

### 测试

- 新增契约边界、非主人只读授权、主人列表不变、同客户端重绑、保存失败不改变运行态、非法字段拒绝和旧主人绑定兼容回归。

## 0.3.3 - 2026-08-09

### 新增

- 系列诊断提供方补齐统一自声明字段，可由“核”动态发现并独立清空。
- 新增 `identity.control_plane@1.0` 可选统一身份控制面，供临以结构化页面原子写入 Quest 主人与精确绑定，并提供仅计数的脱敏状态。

### 修复

- 修复只填写 Quest 五段绑定却未同步 `owner_users`，导致配置保存后仍返回 `owner_not_configured` 的断裂。

### 安全

- 新保存的 API 主体只写入不可逆 SHA-256 摘要；旧明文绑定只读兼容，同客户端再次保存时自动迁移。保存失败或并发版本被覆盖时不改变运行态。
- 控制面存在但拒绝时，消费者不得与本地配置合并提权；自然人绑定继续不授予平台权限。
- 移除测试中遗留的真实平台账号与私人名片样例，统一改为显式测试占位值。

### 测试

- 增加结构化 schema、摘要持久化、旧配置兼容、保存失败不变、重建配置后授权和禁止自然人提权回归测试。

## 0.3.2 - 2026-08-05

### 变更

- 同步系列诊断提供方契约，新增插件无需修改“核”即可被日志页发现。

## 0.3.1 - 2026-08-03

### 新增与变更

- 增加 `identity.quest_session_authorization@1.0` 只读契约，按原始平台身份精确授权 Quest 会话，不把自然人映射或关系分数替代平台权限。
- 配置页说明改为中文大白话，明确主人、管理员、处罚、黑名单、跨群共享和自动审核等设置的风险边界。
- 更正英文 LICENSE 版权署名为 `qsbb`；中文发布作者继续使用“凌溪”。
- 完善系列诊断日志：捕获范围扩展到 `DEBUG`-`CRITICAL`，单插件内存缓冲上限为 1000 条，正文继续脱敏并限制长度。

### 测试

- 增加 Quest 原始身份授权契约、schema 边界和旧配置回归测试。

## 0.3.0 - 2026-07-31

### 头像

- 更新凝心溯溪-序 的插件头像为本次发布的 `logo.png`。

### Added

- 新增 `series.diagnostics@1.0` 只读诊断契约，以进程内环形缓冲向“核”的统一日志页提供必要生命周期事件、警告和错误；未安装“核”时不影响本插件独立运行。

### Changed

- 发布元数据作者与运行时注册作者统一为凌溪。

### Security

- 系列诊断通道与 AstrBot 主日志隔离，记录仅驻留内存；敏感字段会脱敏、过长内容会截断，重启或热重载后缓冲自动清空，且不保存普通聊天正文。
- 自动告警仅暴露结构化定位信息，不保存格式化后的日志正文；清空会轮换诊断流标识，避免新旧实例事件混合。

## [0.2.2] - 2026-07-31

### Added

- 新增 `identity.context_bridge_authorization@1.0` 只读契约，供言逐轮裁定同一自然人的近期片段能否从其他私聊或本人此前群聊进入当前请求。

### Security

- 私聊进入群聊默认拒绝；只有当前群消息自己明确同意时才单轮放行，并区分“只确认相关话题存在”和“允许公开必要细节”。否定、撤回、假设、规则讨论、引用消息中的授权文字及无法确认目标作用域时全部失败关闭。
- 契约只返回授权结果、模式和字符上限，不返回身份或消息数据，不缓存同意，不执行发送，也不把情提供的跨平台自然人归属转化为平台权限。

### Tests

- 新增私聊到私聊、本人群聊到私聊、群聊到群聊拒绝、私聊到群聊两档明示同意、否定/假设及引用伪造防护测试。

## [0.2.1] - 2026-07-30

### Added

- 新增 `identity.proactive_authorization@1.0` 只读契约与 `proactive_delivery_targets` 配置，按完整 UMO 精确授权主人私聊收件目标。

### Security

- 主动消息授权拒绝群聊、频道、未列出目标和跨平台自然人权限继承；契约不暴露 UID、不执行发送，序停止或禁用时严格拒绝。
- UMO 必须具备非空平台、消息类型和会话三段，消息类型仅精确接受 `FriendMessage`、`PrivateMessage`、`DirectMessage`，不再使用可能误收请求事件或相似名称的子串判断。

### Tests

- 新增完整私聊 UMO 放行、未授权目标、空段、请求事件、相似名称、插件禁用及群聊/频道拒绝回归测试。

## [0.2.0] - 2026-07-29

### 修复

- 修复 `notify_only` 入群审核模式仍可能进入自动批准链路的问题；该模式现在只建立待审记录并通知管理员，绝不调用平台通过/拒绝接口。

### 安全

- 明确身份双轨边界：主人、友好、保护、黑名单与群管理授权只比较当前平台事件的原始账号，不接受情提供的自然人 ID 或其他平台账号替代，防止跨平台关系映射转化为权限继承；共享边界摘要只记录权限模式，不写平台 ID 或用户 ID。

### 新增

- 把身份边界与安全规则登记为结构化提示片段，供言稳定排序和去重；保留原直接注入作为独立运行与滚动升级 fallback。

### 测试

- 新增 `notify_only` 禁止自动执行、自然人别名不得获得平台权限，以及提示片段严格校验回归测试。

## [0.1.8] - 2026-07-28

### Security

- `/idg status/stop/resume/reset_breaker/refresh/approve/reject` 全部要求 AstrBot 管理员权限；待确认条目只在原群可见、可批准或拒绝。
- 批准时按审批人的实时身份重新运行策略和安全护栏，校验失败不消费确认记录；延迟自禁言会固定原请求目标，避免误操作审批人。

### Added

- 接入 `ningxin.request_context` 1.0，在身份提示与工具裁剪成功后发布不含用户标识的边界摘要，供后续知识层只读确认执行顺序。
- 声明 `plugin.health@1.0` 供核执行更新后业务健康检查。

### Tests

- 新增管理员装饰器、跨群确认隔离、审批实时重校验及统一请求上下文测试。

## [0.1.7] - 2026-07-28

### Fixed

- 修复知识桥接长期静默失效：入群审核向知（`astrbot_plugin_active_learner`）查询判定证据时，此前按 `hasattr(instance, "recall")` 做 duck-typing 探测，而知从未提供该方法，桥接实际从未生效。探测失败只能得到「没有证据」，与「知里确实没有这条记忆」无法区分，日志里仅一条 info，排查成本极高。现改为显式契约校验。

### Changed

- 知识桥接改为契约制：校验对端 `knowledge_contract()` 声明的契约名与 major 版本，本插件支持 `active_learner.knowledge` 1.x。契约名不匹配、major 不兼容、版本无法解析、声明调用失败或缺少 `recall()` 时，一律 **warning 告警并停用桥接**，不再静默降级；失效原因记录在 `unavailable_reason` 中可直接查看。对端未安装属正常部署形态，仅记 info。
- `on_llm_request` 显式声明 `priority=800`。身份与行动边界属安全层，必须先于知识注入（知 700）、表达约束（情 600）与沉默判断（言 500）生效。此前不声明优先级时实际顺序取决于 AstrBot 插件加载次序，会造成「安全边界在知识注入之后才生效」这类不可复现故障。
- 桥接不可用时入群审核保持待审：既不因取不到证据而放行，也不因桥接异常而拒绝。
- README 不再登记具体版本号：「当前实现信息」与插件信息表中的版本字面量改为指向 `metadata.yaml`。README 里的版本号不随发布自动更新，本次整改前已停留在 `0.1.6`，读者据此判断兼容性反而被误导。

### Tests

- 新增 `tests/test_knowledge_contract.py`，覆盖契约缺失、非 dict 返回、契约名不符、major 不兼容、版本无法解析、缺少 `recall()`、声明抛异常、检索异常等失配场景，并断言失配必须告警且不调用对端。

## [0.1.6] - 2026-07-27

### Changed

- 版本号格式迁移：由带 `v` 前缀的 `v0.1.5` 迁移为三段式无前缀格式 `0.1.6`，与系列其他插件统一。同步更新 `metadata.yaml`、`__init__.py`（`main.py` 注册版本由 `__init__.__version__` 导出，随之生效）与 README 中的当前版本标注。历史版本条目保持原样，不做改写。
- `metadata.yaml` 的 `astrbot_version` 写法与系列统一为 `">=4.17,<5"`（语义与原 `">=4.17.0,<5.0.0"` 等价，下界仍覆盖代码使用的 API）。

## [v0.1.5] - 2026-07-26

### Fixed

- 修复所有写操作实际生效却被报告为失败的问题。典型表现：让 bot 改自己群名片，名片确实改成了，但回复「好像改不了，我没这个权限呢」，日志里是 `Tool set_self_card Result: 执行失败：set_group_card failed`。根因是成功判定读了不存在的字段：aiocqhttp 的 `call_action` 内部已由 `_handle_api_result` 拆包，**只返回 `data` 字段**，并在 `status == "failed"` 时抛 `ActionFailed`；而 OneBot 的写操作成功时 `data` 为 `null`，即 `call_action` 返回 `None`。插件此前用 `resp["status"] == "ok"` 判定，这个分支永远走不到，`None` 被当成失败。现改为「未抛异常即成功」，并保留对返回完整响应信封的适配器的兼容判定（要求同时含 `status` 与 `retcode`，避免把恰好带 `status` 字段的业务 data 误判）。此问题影响改名片、禁言、踢人、改群名、设头衔等全部写操作。
- 补全失败日志的诊断信息：`ActionFailed` 的 `str()` 只带 retcode，真正有用的 `wording` / `msg` 藏在 `result` 字典里，现已一并输出，便于区分权限不足与其他失败原因。

## [v0.1.4] - 2026-07-25

### Fixed

- 修复热重载后事件处理器崩溃：`'IdentityGuardianPlugin' object has no attribute 'get_platform_name'`。旧实例遗留的 `functools.partial` 会把插件实例再绑一次到第一个形参上，导致 `event` 形参整体错位收到插件实例本身。`on_event` 与 `on_llm_request` 现按鸭子类型从全部实参中取回真正的 event 与 ProviderRequest，取不到则安静跳过，不再抛栈污染日志。
- 修复 `_unwrap_registry_handlers` 自始未生效的问题：此前读取上游不存在的 `registry.handlers` 与 `handler.full_name`，前者恒为空、后者恒为空串，导致 partial 拆解从未匹配到任何 handler。现改用上游真实字段 `registry._handlers` 与 `handler_full_name`（缺失时回退 `handler_module_path`），并支持多层套娃一路剥到原始函数，同时严格只处理本插件自己的 handler。
- 修复 `metadata.yaml` 版本号停留在 v0.1.2 导致更新检查失效的问题。v0.1.3 只改了 `__init__.__version__`，而 AstrBot 与更新器读取的是 `metadata.yaml`，因此远端始终报告旧版本、界面上不会出现可更新提示。两处版本号现已同步，并新增回归测试防止再次漂移。

## [v0.1.3] - 2026-07-25

### Fixed

- 修复 bot 为普通成员时无法按请求修改自己群名片的问题。LLM 常把「改你自己的名片」表达为 `set_member_card(user_id=<bot 自己>)`，此前会被管理员权限门以「bot 身份(member)无此权限」拒绝。策略引擎现在在权限校验前把指向 bot 自己的 `set_member_card` 归一化为 `set_self_card`（OneBot 中改自己名片只需 member 权限）。指向他人的调用仍按原权限规则拒绝，不构成提权路径。
- 修复群成员信息查询失败时把降级结果写入缓存的问题。此前一次接口抖动就会把真实的管理员身份在整个 `identity_refresh_interval` 周期内锁死为 `member`，导致本该允许的管理动作被持续拒绝。查询失败或 `role` 字段缺失/不可识别时仍降级为 `member`，但不再写入缓存，下次调用可恢复真实身份。
- 统一 `role` 字段解析，兼容字符串、整数（1/2/3）与对象三种 OneBot 实现差异，并对大小写和空格做归一化；无法识别的取值不再被当作 `member` 缓存。
- 修复 v0.1.2 引入的工具过滤实际未生效的问题。过滤逻辑此前读写 `ProviderRequest.tools`，而 AstrBot 用 `func_tool`（`ToolSet`）承载工具列表，导致过滤恒为空操作，普通成员身份下仍会看到管理员 / 群主工具。现改为在 `func_tool` 上按名称移除不可用工具，并保留对旧 `tools` 列表结构的兼容。该 `ToolSet` 由 AstrBot 每次请求新建，原地移除不会污染全局工具表或其他会话。

### Changed

- 身份上下文新增 bot 自己的 QQ 号，并在行动边界中明确提示改自己名片应使用 `set_self_card`，减少 LLM 选错工具。

## [v0.1.2] - 2026-07-25

### Added

- 按 bot 在当前群的真实身份动态过滤 LLM 工具：普通成员看不到管理员 / 群主工具，管理员看不到群主专属工具，避免 LLM 调用注定失败的接口。过滤只作用于本次请求，不影响其他群与其他插件的工具。
- 补齐开发文档中已定义但未注册的两个只读工具：`get_group_member_info`（查询成员昵称、群名片、角色与头衔）和 `list_group_members`（列出成员总数与管理层名单）。两者无副作用，不写审计与冷却。
- 新增能力与工具注册一致性测试，防止 `CAPABILITY_MAP` 与实际注册的 `llm_tool` 再次出现漂移。

## [v0.1.1] - 2026-07-25

### Changed

- 入群问答配置改为 WebUI 友好的字符串列表，每项使用 `问题|答案1,答案2` 格式，并兼容旧对象格式。
- 统一为“凝心溯溪-序”系列命名与说明。
- OneBot 查询动作失败日志降级为 DEBUG，并补充错误信息与关键诊断参数。

### Fixed

- 补充 `metadata.yaml` 的仓库与主页地址，使 AstrBot 插件更新器能够定位远程仓库。
- 明确 QQ 号列表、会话 ID 与入群问答配置的数据格式，补齐 AstrBot list schema 的 `items` 定义。
- 修复 OneBot 写操作成功返回 `data=null` 时被误判为失败的问题，避免群名片实际修改成功后 LLM 错误重试。

## [v0.1.0] - 2026-07-23

### Added

- 身份识别：bot、发送者与目标在群中的 role（owner / admin / member）查询与缓存。
- 关系识别：主人 / 友好用户 / 普通群员关系解析，注入到 LLM 上下文。
- 行动边界注入：`on_llm_request` 钩子向 LLM 注入当前身份、关系、允许行动集与安全规则。
- 14 个 LLM 工具：`mute_current_sender`、`request_self_mute`、`mute_member`、`unmute_member`、`kick_member`、`delete_message`、`set_member_card`、`set_self_card`、`set_member_title`、`set_group_name`、`set_group_admin`、`set_whole_ban`、`approve_join_request`，按身份分级暴露并由策略引擎硬校验。
- 三层安全防护：规则预筛（关键词/刷屏）+ 独立 LLM 审核 + API 层硬拦截（白名单/频率/批量/熔断）。
- 入群审核：读取 QQ 申请问题与答案，结合配置答案与可选知识库，仅高置信度时自动通过；错误、不确定或联动失败保持待审，不自动拒绝。
- 内容审核：独立于主对话的 LLM 内容审核，违规分级处罚（默认关闭）。
- 防刷屏：同一用户 10 秒内消息数超阈值直接禁言（可配置）。
- 二次确认：高破坏性操作（踢人、全员禁言、改群名、任命管理员）走人工确认队列。
- 熔断器：1 小时内管理操作总数超阈值自动停用所有高风险工具，需 `/idg reset_breaker` 手动恢复。
- 审计日志：所有有副作用的行动写入 `audit-YYYYMMDD.jsonl`，按日切割。
- 紧急停止：`/idg stop` 一键停用所有管理工具，仅保留身份注入。
- 知识库联动：入群审核可查询 active_learner 知识库作为判定证据，失败时保持待审。
- bot 进群欢迎：监听 `group_increase` 且 `user_id == self_id` 时触发。
- bot 退群清理：监听 `group_decrease` 且 `user_id == self_id` 时清空身份缓存。
- 管理员变更刷新：监听 `notify.group_admin_change` 通知时刷新身份缓存。
- 后台任务：`initialize()` 启动身份缓存定时刷新循环与待确认条目过期清理循环。
- `/idg` 指令组：`status` / `stop` / `resume` / `reset_breaker` / `refresh` / `approve` / `reject` / `help`。
- 单元测试：59 个用例覆盖策略引擎、能力映射、配置解析、冷却与熔断、内容审核、入群审核。

### Security

- 三层防御架构确保即使 LLM 被完全注入也无法绕过 API 层硬拦截。
- 普通成员请求影响第三人时由代码层拒绝，不构成授权。
- 主人、群主、管理员默认免疫踢出与长时禁言；可配置严格限时的玩笑式短禁言例外。
- 所有身份信息来自平台事件，不信任聊天文本中的身份声明。
- 入群审核页补齐弹窗焦点回归、可访问状态、触屏 hover 隔离与 Bridge 初始化超时反馈。
